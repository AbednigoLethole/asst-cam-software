/* JavaScript can be used to dynamically position the tooltip
const modebutton = document.getElementById('modeState');
const tooltip = document.querySelector('.tooltip');
const modebuttonWidth = modebutton.offsetWidth;
tooltip.style.marginLeft = `-${modebuttonWidth / 2}px`;

const stowbutton = document.getElementById('stowState');
const tooltip = document.querySelector('.tooltip');
const stowbuttonWidth = stowbutton.offsetWidth;
tooltip.style.marginLeft = `-${stowbuttonWidth / 2}px`;

const funcbutton = document.getElementById('funcState');
const tooltip = document.querySelector('.tooltip');
const funcbuttonWidth = funcbutton.offsetWidth;
tooltip.style.marginLeft = `-${funcbuttonWidth / 2}px`;
*/